# Fast-Api-app

Simple FastAPI startup boilerplate.

## Local development

```bash
make dev       # run FastAPI locally with hot reload (port 2000)
```

If you found issues while make dev,  add below in terminal
chmod +x scripts/dev.sh

App runs at http://localhost:2000
